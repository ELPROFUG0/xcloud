{
  "name": "agent-studio",
  "version": "0.1.0",
  "private": true,
  "description": "IDE de agentes de IA — Agent Studio",
  "scripts": {
    "poc": "tsx scripts/poc-websocket.ts",
    "remote:assets": "bash scripts/prepare-remote-setup-assets.sh",
    "test": "pnpm -r test",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  }
}
